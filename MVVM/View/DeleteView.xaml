<UserControl x:Class="TranslateAppWPF.MVVM.View.DeleteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TranslateAppWPF.MVVM.View"
             xmlns:vm="clr-namespace:TranslateAppWPF.MVVM.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:DeleteViewModel/>
    </UserControl.DataContext>
    
    
    <Grid>

        <Label x:Name="Label1"
               Content="Usuwanie zestawu"
               Style="{StaticResource LabelTheme}"/>

        <StackPanel Margin="300,250,300,10">
            <ComboBox Width="200"
                      SelectedItem="{Binding SelectedFile}"
                      ItemsSource="{Binding Files}"/>
            
            <Button Content="Usuń zestaw"
                    Margin="0, 150, 0, 0"
                    Command="{Binding DeleteCommand}"
                    Style="{StaticResource NewButton}"/>
        </StackPanel>
    </Grid>
</UserControl>
